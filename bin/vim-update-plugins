#!/bin/bash

PLUGIN_FOLDER="$HOME/.vim/bundle"
if [ "$1" ]; then
  PLUGIN_FOLDER="$1"
fi

for PLUGIN in $PLUGIN_FOLDER/*; do
  cd $PLUGIN
  echo "Updating $PLUGIN"
  git pull
  cd $PLUGIN_FOLDER
done
